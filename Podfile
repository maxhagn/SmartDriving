# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'smartdriving' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for smartdriving
  pod 'Firebase/Core'
  pod 'Firebase/Database'
  pod 'FirebaseUI'
  
  target 'smartdrivingTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'smartdrivingUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
